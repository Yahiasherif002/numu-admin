Table footer control — the range readout matters more than the page numbers in an operations tool.

```jsx
<Pagination page={page} pageSize={50} total={12480} onPageChange={setPage} onPageSizeChange={setSize} />
```

Prev/Next chevrons mirror automatically under RTL.
