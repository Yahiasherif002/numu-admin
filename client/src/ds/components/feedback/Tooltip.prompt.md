Reveals a truncated or abbreviated value — a full timestamp behind "4m ago", a full ID behind a short one.

```jsx
<Tooltip mono label="2026-09-08 11:04:22 EET"><span className="numu-mono">4m ago</span></Tooltip>
```

Never put information an operator needs to act on in a tooltip; it is unreachable on touch and easy to miss.
