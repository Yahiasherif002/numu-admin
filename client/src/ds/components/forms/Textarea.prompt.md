Multi-line input — internal notes, support replies, incident updates.

```jsx
<Textarea placeholder="Internal note — visible to NUMU staff only" rows={4} />
<Textarea arabic placeholder="اكتب ملاحظتك…" />
```

Vertical resize only, so it can never break a two-column form grid.
