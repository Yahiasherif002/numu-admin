Small uppercase mono pill for a state or a label; the base primitive under `StatusBadge`.

```jsx
<Badge tone="success" icon="check">Paid</Badge>
<Badge tone="neutral" square>COD</Badge>
```

Status is never communicated by colour alone — carry a word, and add `icon` or `dot` when the word alone is thin.
